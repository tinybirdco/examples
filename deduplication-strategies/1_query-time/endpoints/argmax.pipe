NODE argMax_endpoint
SQL >

    SELECT
        max(timestamp) last_timestamp,
        id, 
        argMax(views, timestamp) views,
        argMax(likes, timestamp) likes,
    argMax(dislikes, timestamp) dislikes
    FROM videos
    GROUP BY id
    ORDER BY last_timestamp, id
